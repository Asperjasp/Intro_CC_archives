#include <stdio.h>

void main()

    int age; /* Cuando se declara una variable de tipo entero , flotante o char se le asigna una dirección de memoria , como hace C scanf para saber en que dirección guardara el valor ingresado ? mediante el operador & (por referencia) , para un arreglo o Un String , la variable tendrá también la dirección del primer elemento ya no hace falta pasar el & ya que
     sabrá donde empezará a escribir el valor o Valores */
printf("Month No");
scanf("%d", &n)
    // El % indica el tipo de variable a escanear en este caso decimal
    // Y el &  indicar una dirección de memoria de la variable donde se almacenará el dato

    swtich(age)
{
case 1:
printf("You are one") case 2:
printf("Your are two") break case 3:
printf("You are three") break case 4:
    printf("Your are three or four") break default : printf("You are not 1,2,3 or 4")
}
