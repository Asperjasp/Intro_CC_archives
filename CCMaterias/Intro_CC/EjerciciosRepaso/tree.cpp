#include <stdio.h>
#include <math.h>
#include <iostream>
#include <vector>

// IN a tree you can link to multiple nodes
// There are not two references that link to the same node
// Violate the definition ov being a 
class Node
{
    int data;
    Node child1;
    Node child2;
    Node child3; // When it doestno exist you can send a null
    // A tree cannot contain Cicles
    

} int main(void)
{

    return 0;
}